<body>
  <header>
    <nav id="nav-bar">
      <a href="/"> ← Go back</a>
    </nav>
  </header>
  <div>
    <h1>artist: {{artist}}</h1>
    <h2>id: {{id}}</h2>
    <div class="main-image">
        <img src="{{url}}" alt={{name}}>
    </div>
    <div id="button-albums-picture">
      <a href="/albums/{{id}}?artist={{artist}}" method="GET" name="artistId">See albums</a>
      {{!-- the method and name are not necessary for the app to work since the route already knows it GET type --}}
    </div>
  </div>
</body>

